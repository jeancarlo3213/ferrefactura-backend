[phases.setup]
nixPkgs = [
    "python310",
    "postgresql_16",
    "libmysqlclient",
    "gcc",
    "unixODBC"
]

[phases.install]
cmds = [
    "python3 -m venv /opt/venv",  # Crea el entorno virtual
    "/opt/venv/bin/python -m pip install --upgrade pip",  # Usa pip con ruta absoluta
    "/opt/venv/bin/pip install -r requirements.txt"  # Usa pip con ruta absoluta
]

[start]
cmd = "/opt/venv/bin/gunicorn --workers=4 FerreFac.wsgi:application --bind 0.0.0.0:8000"
